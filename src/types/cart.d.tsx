//This code defines the types of items in my cart. 
//the id has to be a number; the name has to be text of some kind; the quantity has to
//be a number; and the price has to be a number

export interface CartItem {
    id: number;
    name: string;
    quantity: number;
    price: number;
  }
  